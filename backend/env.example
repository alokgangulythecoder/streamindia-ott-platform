# StreamIndia Backend Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb+srv://admin:L7yBWtFsHNCRLwhK@cluster0.ld2zdtw.mongodb.net/?appName=Clustor0/classicflims

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=classicfilems-for-all

# File Upload Configuration (Optional)
MAX_FILE_SIZE=104857600
UPLOAD_DIR=./uploads

# CORS Configuration (Optional)
CORS_ORIGIN=http://localhost:8080

# Session Configuration (Optional)
SESSION_SECRET=another-secret-for-sessions

# Email Configuration (Optional - for future features)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Payment Gateway (Optional - for future features)
RAZORPAY_KEY_ID=your_razorpay_key
RAZORPAY_KEY_SECRET=your_razorpay_secret

# Cloud Storage (Optional - for future features)
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=streamindia-media

# CDN Configuration (Optional)
CDN_URL=https://cdn.streamindia.com

# Analytics (Optional)
GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# Admin Configuration
ADMIN_EMAIL=admin@streamindia.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Security
BCRYPT_ROUNDS=10
JWT_EXPIRY=24h

# Rate Limiting (Optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging (Optional)
LOG_LEVEL=info
LOG_FILE=./logs/app.log
